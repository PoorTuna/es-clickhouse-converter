apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecc.ui.fullname" . }}
  labels:
    {{- include "ecc.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
  {{- with (merge (deepCopy .Values.ui.service.annotations) .Values.commonAnnotations) }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - name: http
      port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ecc.selectorLabels" (dict "root" . "component" "ui") | nindent 4 }}
